[(if (> x 0) :pos)
 (if (> x 0) :pos :neg)
 (when (> x 0) (println x))
 (do (step1) (step2))
 (cond (< x 0) :neg (= x 0) :zero :else :pos)
 (case x 1 :one 2 :two :other)
 (for [x xs] (* x 2))
 (for [x xs :when (even? x)] x)
 (try (risky) (catch Exception e (handle e)))
 (try (f) (finally (cleanup)))
 (let [x 5] (* x 2))
 (defn f [x] (+ x 1))
 (defn g [x] (let [y (+ x 1)] (* y 2)))
 (defn greet ([] "Hello!") ([name] (str "Hello, " name)))
 (loop [acc 0 i 0] (if (>= i 10) acc (recur (+ acc i) (+ i 1))))]
